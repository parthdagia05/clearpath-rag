Place your PDF documents here.

The ingestion pipeline (scripts/ingest.ts) will read all .pdf files
from this directory and chunk them for the ClearPath knowledge base.

Usage:
  cd backend && npx ts-node scripts/ingest.ts
